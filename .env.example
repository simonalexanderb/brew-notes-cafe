# Port Configuration for Docker
# Change these ports if you have conflicts with existing services

# Frontend port (default: 8080)
FRONTEND_PORT=8081

# Backend port (default: 8000)
BACKEND_PORT=8001
